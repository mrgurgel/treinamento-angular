import { MinLengthArrayDirective } from './min-length-array.directive';

describe('MinLengthArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new MinLengthArrayDirective();
    expect(directive).toBeTruthy();
  });
});
